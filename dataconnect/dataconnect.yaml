specVersion: "v1beta"
serviceId: "financeapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "financeapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
